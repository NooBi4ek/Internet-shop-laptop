export interface Phone {
  id: number;
  name: string;
  short_desc: string;
  img: string;
  thread_name: string;
  front_camera: string | number;
  main_camera: string | number;
  count: string | number;
  quantity_threads: number;
  memory: number;
  price: number;
  click: boolean;
}
